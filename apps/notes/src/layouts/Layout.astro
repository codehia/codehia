---
import { name, themeColor, twitterHandle } from 'spectre:globals';
import Layout from '@codehia/ui/Layout.astro';
import { siteUrls } from '@codehia/ui/site-urls';

const navLinks = [
	{ label: 'Blog', href: siteUrls.blog },
	{ label: 'Projects', href: siteUrls.projects },
	{ label: 'Notes', href: '/', active: true },
];

interface Props {
	title: string;
	description?: string;
	image?: ImageMetadata;
	pagefindIgnore?: boolean;
	article?: {
		createdAt: Date;
		updatedAt?: Date;
	};
}
---

<Layout
	{...Astro.props}
	name={name}
	themeColor={themeColor}
	twitterHandle={twitterHandle}
	siteTitleHref={siteUrls.main}
	navLinks={navLinks}
>
	<slot name="left" slot="left" />
	<slot name="right" slot="right" />
	<slot name="404" slot="404" />
</Layout>
